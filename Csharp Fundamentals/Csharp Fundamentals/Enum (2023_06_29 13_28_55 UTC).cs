using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.Intrinsics.X86;
using System.Text;
using System.Threading.Tasks;


/*An enum is a special "class" that represents a group of constants (unchangeable/read-only variables). To create an enum, use the enum keyword (instead
 * of class or interface), and separate the enum items with a comma: Enum is short for "enumerations", which means "specifically listed".
 You can also have an enum inside a class:*/
namespace Csharp_Fundamentals
{
    enum Level
    {
        Low,
        Medium,
        High
    }


}
